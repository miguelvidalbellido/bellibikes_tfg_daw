const authBase = 'maintenance'

const ScannerConstants = {
    'ASOCIATE': `${authBase}/associatedScanner`,
    'CHECKSCANNERUSER': `${authBase}/associatedScanner`,
    'GETINCIDENTSSCAN': `${authBase}/scannerData`,
}

export default ScannerConstants