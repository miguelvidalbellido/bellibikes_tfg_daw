const authBase = 'incidents'

const IncidentConstants = {
    'CREATE': `${authBase}/su`,
    'GETALL': `${authBase}`,
    'GET': `${authBase}`,
    'UPDATE': `${authBase}`,
}

export default IncidentConstants