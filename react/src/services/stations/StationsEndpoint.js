const authBase = 'stations/'

const StationConstants = {
    'CREATE': `${authBase}su`,
    'GET': `${authBase}`,
    'UPDATE': `${authBase}su`,
    'DELETE': `${authBase}su`,
}

export default StationConstants