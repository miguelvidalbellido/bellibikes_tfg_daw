const authBase = 'bikes/'

const BikeConstants = {
    'CREATE': `${authBase}su`,
    'GET': `${authBase}`,
    'UPDATE': `${authBase}su`,
    'DELETE': `${authBase}su`,
}

export default BikeConstants