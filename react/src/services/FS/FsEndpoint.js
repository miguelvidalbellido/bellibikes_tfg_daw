const authBaseFS = '/productos'
const authBase = '/users'

const FsConstants = {
    'GET': `${authBase}`,
    'GETFS': `${authBaseFS}`,
    'POST': `${authBase}/addPlan`
}

export default FsConstants