const rentBase = ''

const RentConstants = {
    'CREATE': `${rentBase}start_rent`,
    'GET': `${rentBase}get_rents`,
    'UPDATE': `${rentBase}su`,
    'DELETE': `${rentBase}su`,
    'GETONE': `${rentBase}getDataRent`,
}

export default RentConstants