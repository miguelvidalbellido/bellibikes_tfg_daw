const authBase = 'slots/'

const SlotConstants = {
    'CREATE': `${authBase}su`,
    'GET': `${authBase}`,
    'UPDATE': `${authBase}su`,
    'DELETE': `${authBase}su`,
}

export default SlotConstants